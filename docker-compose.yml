version: "3.9"
services:
  dev:
    # Will build ./Dockerfile
    # This Dockerfile is for development
    build:
      context: .
      dockerfile: ./Dockerfile
      image: technosaby/gsoc2022-redhen-audio-tagging
    volumes:
      - ./:/TaggingAudioEffects
